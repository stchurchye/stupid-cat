# Fixtures

Optional test media for Mac development without live RTSP:

- `sample_ir.mp4` — short IR-domain clip of the litter box (not committed; add locally).

Tests that need video should skip if the file is missing.
